create table todo (
                      id int not null AUTO_INCREMENT,
                      description varchar(2000),
                      complete boolean,
                      primary key (id)
);